{
  "name": "root",
  "private": true,
  "devDependencies": {
    "jest": "^27.1.0",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "eslint": "^7.32.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.3.2"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint --ext .js packages/**/*.js  --no-error-on-unmatched-pattern --fix "
  }
}
